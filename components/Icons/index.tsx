import Arrow from "./Arrow";
import Netflix from "./Netflix";
import Plus from "./Plus";

export type IconProps = {
  size?: number;
  color?: string;
  onClick?: () => void;
};

export const Icons = {
  Arrow,
  Netflix,
  Plus,
};
